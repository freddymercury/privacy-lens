# PrivacyGuard Backend Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (used for cookie encryption)
SESSION_SECRET=your-secret-key-here

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# LLM Configuration
OPENAI_API_KEY=your-openai-api-key
LLM_MODEL=gpt-4 # or another model supported by llamaindex

# Assessment Configuration
ASSESSMENT_TRIGGER_INTERVAL_MINUTES=600 # How often to check for unassessed URLs (in minutes)
MAX_CONCURRENT_ASSESSMENTS=1 # Maximum number of concurrent URL assessments to avoid rate limits (default: 1)
